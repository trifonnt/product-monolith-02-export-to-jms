version: '2'
services:
    product-postgresql:
        image: postgres:9.6.2
        # volumes:
        #     - ~/volumes/jhipster/product/postgresql/:/var/lib/postgresql/
        environment:
            - POSTGRES_USER=product
            - POSTGRES_PASSWORD=
        ports:
            - 5432:5432
